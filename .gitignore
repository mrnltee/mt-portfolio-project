node_modules/
# Generated token outputs — never hand-edited, always rebuilt from src/.
packages/*/build/
*.log
.DS_Store
.vercel
.env*
# Agent skills installed via `npx skills` — per-machine tooling, not app code.
.agents/
.claude/skills/
skills-lock.json
